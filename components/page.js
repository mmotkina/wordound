import React from 'react';
import Wordound from './wordound';

const Page = React.createClass({
    render() {
        return (
            <div className="page">
                <Wordound />
            </div>
        );
    }
});

export default Page;
